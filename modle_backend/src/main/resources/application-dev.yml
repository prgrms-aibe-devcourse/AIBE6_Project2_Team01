spring:
  datasource:
    url: jdbc:mysql://localhost:3306/modle?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    gcp:
      storage:
        enabled: false

jwt:
  secret: ${JWT_SECRET}

gcs:
  bucket: ${GCS_BUCKET_NAME:local-dummy}